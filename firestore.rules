rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Subscription, usage, routing and billing data are backend-only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
