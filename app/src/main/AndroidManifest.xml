<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- Queries para detectar apps instaladas (Android 11+) -->
    <queries>
        <!-- Required on Android 11+ to discover speech recognition and TTS services. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- Spotify -->
        <package android:name="com.spotify.music" />
        <!-- Google Keep -->
        <package android:name="com.google.android.keep" />
        <!-- Google Play Music (legacy) -->
        <package android:name="com.google.android.music" />
        <!-- YouTube Music -->
        <package android:name="com.google.android.apps.youtube.music" />
        <!-- Intent genérico para apps de música -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="spotify" />
        </intent>
        <!-- Intent para crear notas -->
        <intent>
            <action android:name="com.google.android.keep.action.CREATE_NOTE" />
        </intent>
        <!-- Intent para abrir URLs en navegador (OAuth) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Intent for launching any app -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Apps populares para open_app -->
        <!-- Mensajería -->
        <package android:name="com.whatsapp" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.facebook.orca" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.discord" />
        <package android:name="com.Slack" />
        <package android:name="com.microsoft.teams" />
        <!-- Redes sociales -->
        <package android:name="com.instagram.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.twitter.android" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.reddit.frontpage" />
        <package android:name="com.pinterest" />
        <package android:name="com.linkedin.android" />
        <package android:name="com.tumblr" />
        <!-- Streaming y entretenimiento -->
        <package android:name="com.google.android.youtube" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.amazon.avod.thirdpartyclient" />
        <package android:name="com.disney.disneyplus" />
        <package android:name="com.hbo.hbonow" />
        <package android:name="com.crunchyroll.crunchyroid" />
        <package android:name="tv.twitch.android.app" />
        <!-- Videollamadas -->
        <package android:name="us.zoom.videomeetings" />
        <package android:name="com.google.android.apps.meetings" />
        <!-- Navegadores -->
        <package android:name="com.android.chrome" />
        <package android:name="org.mozilla.firefox" />
        <package android:name="com.brave.browser" />
        <package android:name="com.opera.browser" />
        <package android:name="com.microsoft.emmx" />
        <!-- Google apps -->
        <package android:name="com.google.android.gm" />
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.google.android.apps.photos" />
        <package android:name="com.google.android.apps.docs" />
        <package android:name="com.google.android.apps.nbu.files" />
        <package android:name="com.google.android.contacts" />
        <package android:name="com.google.android.dialer" />
        <package android:name="com.google.android.apps.messaging" />
        <package android:name="com.google.android.calculator" />
        <package android:name="com.google.android.deskclock" />
        <package android:name="com.google.android.calendar" />
        <package android:name="com.google.android.apps.translate" />
        <!-- Productividad y oficina -->
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.microsoft.office.word" />
        <package android:name="com.microsoft.office.excel" />
        <package android:name="com.notion.id" />
        <package android:name="com.todoist" />
        <package android:name="md.obsidian" />
        <!-- Transporte y delivery -->
        <package android:name="com.ubercab" />
        <package android:name="com.ubercab.eats" />
        <package android:name="com.dd.doordash" />
        <package android:name="com.rappi" />
        <!-- Finanzas -->
        <package android:name="com.paypal.android.p2pmobile" />
        <package android:name="com.mercadolibre" />
        <package android:name="com.mercadopago.wallet" />
        <!-- Fitness y salud -->
        <package android:name="com.myfitnesspal.android" />
        <package android:name="com.strava" />
        <!-- Utilidades -->
        <package android:name="com.shazam.android" />
        <package android:name="com.android.vending" />
    </queries>

    <application
        android:name=".AIAgentsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIAgentsApp"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Collection is enabled after encrypted user preferences are available. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AIAgentsApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".presentation.assistant.CortexAssistantActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:theme="@style/Theme.AIAgentsApp.CortexAssistant">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          The system invokes the selected VoiceInteractionService for assistant gestures
          (including a long press of the power button on Pixel devices). ACTION_ASSIST on
          the activity is kept as a compatibility path for launchers that use an intent.
        -->
        <service
            android:name=".presentation.assistant.CortexVoiceInteractionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:process=":cortex_voice">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/cortex_voice_interaction_service" />
        </service>

        <service
            android:name=".presentation.assistant.CortexVoiceInteractionSessionService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <!-- Required metadata companion for VoiceInteractionService on Android 11 and older. -->
        <service
            android:name=".presentation.assistant.CortexRecognitionService"
            android:exported="true"
            android:permission="android.permission.BIND_SPEECH_RECOGNITION_SERVICE"
            android:process=":cortex_voice">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/cortex_recognition_service" />
        </service>
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".data.terminal.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.terminal.TaskPermissionActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.scheduling.ScheduledTaskReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.scheduling.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".data.speech.VoicePackInstallReceiver"
            android:exported="false" />

        <service
            android:name=".data.background.CortexForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!-- Disable default WorkManager initializer to use Hilt configuration -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- WorkManager SystemForegroundService with dataSync type -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
